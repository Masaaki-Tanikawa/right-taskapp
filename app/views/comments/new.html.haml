.inner.inner-form
  %h2.comment-title 新しいコメントを作成
  %ul
    - @comment.errors.full_messages.each do |message|
      %li= message

  = form_with(model: @comment, url: board_card_comments_path(@board, @card), local: true) do |f|
    .form-field
      = f.label  :content,class: 'form-label'
      = f.text_area :content,class: 'form-text text-area'
    .form-actions
      = f.submit '保存', class: 'btn-primary'